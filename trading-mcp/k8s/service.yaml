apiVersion: v1
kind: Service
metadata:
  name: trading-mcp
  namespace: trading-mcp
  labels:
    app: trading-mcp
spec:
  type: ClusterIP
  selector:
    app: trading-mcp
  ports:
    - name: http
      port: 8765
      targetPort: http
