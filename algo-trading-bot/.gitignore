# Fetched market data — reproducible via `atb fetch`, not source.
# Anchored to the project root so it does NOT match the src/.../data package.
/data/

# Backtest run outputs / reports
/runs/
*.parquet

# Live/paper runtime state + decision logs
/state/

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.mypy_cache/
.ruff_cache/
