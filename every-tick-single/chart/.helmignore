# Keep deployment package small; logs/artifacts must not be shipped in chart.
update-log/

# Local artifacts
__pycache__/
files/scripts/__pycache__/
*.pyc
.DS_Store

# Optional local notes
ANALYSIS_WORKFLOW.md

pm-btc-logs_*

# Model files are managed by apply-model-configmap.sh, NOT shipped in the
# chart — keeps the Helm release Secret under the 1 MiB Kubernetes cap.
files/model/
manifests/